# CLAUDE.md

- 배포 요청("배포해줘", "GitHub Pages에 올려줘" 등)을 받으면 README.md의 "배포 (GitHub Pages)" 절차를 따른다.
- 게임에 새 파일이나 폴더를 추가하면 `.github/workflows/deploy-pages.yml`의 `Collect game files` 단계에도 추가한다.
- 공개 저장소다. 개인 메모 사진(`IMG_*` 등)이나 비밀 정보는 커밋하지 않는다.
